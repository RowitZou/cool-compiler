class A {

  kkk : Int;
  
  test (): BOOL {
    let a : Int , b : Int 
       in let c : Int <- 10 
            in 
             false 
  };
};

Class BB__ inherits A {
    ll : Int <- 1;

    test1 (var : Int) : BOOL{
     {
   2<4;
   if(3<1) then var <- 1 else if(3<1) then var <-1 else 
     var <- 0+0+0+0+0+0+0+0 fi fi; 
    new A;
    while var=1 loop {var = var *2;
    let a:Int ,b : Int <- 10 in false;

}
pool;
}
   
};
    test(var : Int ) : BOOL{
   case (ll) of
    var : Int => case (ll) of 
              var : Int => ll1 <- 8;
               esac;
    esac
};

};

class Silly {
  copy(): SELF_TYPE {self };

};

(*sbfdbdfbdfbdfbdf*)


